server:
  servlet:
    encoding:
      force: 'true'
      charset: utf-8
      enabled: 'true'

spring:
# config:
#   activate:
#     on-profile:
#       local, deploy

h2:
  console:
    enabled: 'true'
jackson:
  serialization:
    FAIL_ON_EMPTY_BEANS: 'false'
datasource:
  url: jdbc:h2:mem:testdb;MODE=MYSQL;
jpa:
  hibernate:
    ddl-auto: update
  show-sql: 'true'
cloud:
  aws:
    credentials:
      access-key: asdfasdf
      secret-key: asdasfadf
    region:
      static: ap-northeast-2
    stack:
      auto: 'false'
    s3:
      bucket: asdfsdaf

jwt:
  secret: asdfsadfsdfsdf